# Url of the bbb instance
url = 'https://bbb.example.de/'

# Room ids to choose from
room_ids = ["room-1", "room-2", "room-3"]

# Settings for flask
flask = {
    # "DEBUG": True,
    "CACHE_TYPE": "SimpleCache",
    "CACHE_DEFAULT_TIMEOUT": 30
}
